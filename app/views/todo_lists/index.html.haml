

%table
  %tr
    %th
      Name
  - @todo_lists.each do |list|
    %tr 
      %td= link_to list.name, todo_list_path(list)
      %td= link_to "Edit", edit_todo_list_path(list)
      %td= link_to "Delete", todo_list_path(list), :method => :delete
      
