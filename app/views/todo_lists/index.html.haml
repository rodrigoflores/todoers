
%h2 You are Watching
  
%table  
  - @watched_lists.each do |watched_list|
    %tr
      %td= link_to watched_list.name, user_todo_list_path(watched_list.user_id, watched_list.id)
      %td= link_to "Unwatch", unwatch_todo_list_todo_list_path(watched_list), :method => 'delete', :class => "unwatch_link"
      


%p= link_to 'New todo-list', new_todo_list_path

%h2 To-do lists created by you

%table
  %tr
    %th
      Name
  - @todo_lists.each do |list|
    %tr 
      %td= link_to list.name, todo_list_path(list)
      %td= link_to "Edit", edit_todo_list_path(list)
      %td= link_to "Delete", todo_list_path(list), :method => :delete
      
